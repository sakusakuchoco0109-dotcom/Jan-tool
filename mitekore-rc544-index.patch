--- index.html.base
+++ index.html
@@ -140,9 +140,9 @@
 })();</script>
 <meta charset="utf-8"/>
 <meta content="width=device-width,initial-scale=1" name="viewport"/>
-<title>みてコレ！ V20.2.35ex RC1.4.542 配布版</title>
+<title>みてコレ！ V20.2.35ex RC1.4.544 配布版</title>
 <link rel="preload" href="assets/base.css" as="style">
-<link rel="preload" href="assets/app-core-542.js" as="script">
+<link rel="preload" href="assets/app-core-544.js" as="script">
 <link rel="preload" href="assets/mitekore-logo.webp" as="image" type="image/webp">
 <link href="https://fonts.googleapis.com" rel="preconnect"/>
 <link crossorigin="" href="https://fonts.gstatic.com" rel="preconnect"/>
@@ -308,13 +308,13 @@
   }
 
   #quickProfileEditor:not(.hidden) .dq-editor-title{
-    position:sticky !important;
-    top:-13px !important;
-    z-index:2 !important;
-    margin:-13px -13px 12px !important;
-    padding:13px !important;
-    background:#192734 !important;
-    border-bottom:1px solid rgba(53,35,79,.12) !important;
+    position:static !important;
+    top:auto !important;
+    z-index:auto !important;
+    margin:0 0 12px !important;
+    padding:0 0 11px !important;
+    background:transparent !important;
+    border-bottom:1px solid rgba(93,78,112,.42) !important;
   }
 
   #quickProfileEditor:not(.hidden) .profile-status-row{
@@ -1954,6 +1954,33 @@
   text-align:center;
 }
 .profile-unlock-overlay.show .profile-unlock-dialog{transform:translateY(0) scale(1)}
+/* RC1.4.544: 二つ名獲得通知は作業を止めないフローティング通知へ。 */
+.profile-unlock-overlay.profile-unlock-nonblocking{
+  inset:auto 16px calc(18px + env(safe-area-inset-bottom,0px)) auto!important;
+  width:min(380px,calc(100vw - 32px))!important;
+  height:auto!important;
+  display:block!important;
+  padding:0!important;
+  background:transparent!important;
+  pointer-events:none!important;
+}
+.profile-unlock-overlay.profile-unlock-nonblocking .profile-unlock-dialog{
+  width:100%!important;
+  max-height:min(55dvh,520px)!important;
+  padding:16px!important;
+  pointer-events:auto!important;
+  text-align:left!important;
+  box-shadow:0 18px 55px rgba(0,0,0,.45)!important;
+}
+.profile-unlock-overlay.profile-unlock-nonblocking .profile-unlock-dialog h2{font-size:18px!important;}
+.profile-unlock-overlay.profile-unlock-nonblocking .profile-unlock-list{max-height:220px!important;overflow:auto!important;}
+@media(max-width:700px){
+  .profile-unlock-overlay.profile-unlock-nonblocking{
+    right:8px!important;left:8px!important;bottom:calc(82px + env(safe-area-inset-bottom,0px))!important;width:auto!important;
+  }
+  .profile-unlock-overlay.profile-unlock-nonblocking .profile-unlock-dialog{max-height:45dvh!important;}
+}
+
 .profile-unlock-spark{
   display:inline-flex;
   align-items:center;
@@ -13461,7 +13488,7 @@
   }
 
   global.FujiyaQr = {render:render,toSvg:toSvg};
-})(window);</script><script src="assets/app-core-542.js"></script><script id="fujiyaMobileTabEmergencyFix">
+})(window);</script><script src="assets/app-core-544.js"></script><script id="fujiyaMobileTabEmergencyFix">
 (function(){
   'use strict';
 
@@ -15031,6 +15058,7 @@
   let externalPreviewCooldownUntil=0;
   let bodyBlocks=[];
   let currentReaderArticle=null;
+  let magazinePublishBusy=false;
   const magazineArticleDetailCache=new Map();
   const MAGAZINE_ARTICLE_CACHE_PREFIX='fujiya_mitemaga_article_session_v1:';
   const MAGAZINE_ARTICLE_PERSIST_KEY='fujiya_mitemaga_article_persist_v2041';
@@ -16069,16 +16097,21 @@
       authorAvatar:profile.avatar
     };
 
+    if(magazinePublishBusy){
+      setEditStatus('公開処理中です。完了まで少しお待ちください。');
+      return;
+    }
+    magazinePublishBusy=true;
+    const publishButton=$m('miteMagazinePublishBtn');
+    if(publishButton){
+      publishButton.disabled=true;
+      publishButton.setAttribute('aria-busy','true');
+      const label=publishButton.querySelector('.mitemaga-publish-label');
+      if(label)label.textContent='公開中…';
+    }
     setEditStatus('投稿先を確認しています…');
 
-    const processingToken=window.fujiyaProcessing?.start({
-      title:'みてマガへ公開しています',
-      detail:'投稿を共有DBへ送信し、公開一覧への反映を確認しています。',
-      progressText:'公開反映を確認中'
-    })||'';
-
     try{
-      await window.fujiyaProcessing?.paint?.();
 
       let gasVersion='';
       let gasBlocksReady=false;
@@ -16152,7 +16185,9 @@
         );
       }
 
-      await fetchArticles(true,{force:true});
+      // 公開自体が完了したら編集画面を解放し、一覧再取得はバックグラウンドで行う。
+      // 一覧のGAS受信が遅い時も30秒以上フル画面を塞がない。
+      fetchArticles(true,{force:true}).catch(error=>console.warn('みてマガ公開後一覧更新失敗',error));
 
       if(typeof bridge.toast==='function'){
         bridge.toast(
@@ -16174,7 +16209,14 @@
         'bad'
       );
     }finally{
-      window.fujiyaProcessing?.end(processingToken);
+      magazinePublishBusy=false;
+      const publishButton=$m('miteMagazinePublishBtn');
+      if(publishButton){
+        publishButton.disabled=false;
+        publishButton.removeAttribute('aria-busy');
+        const label=publishButton.querySelector('.mitemaga-publish-label');
+        if(label)label.textContent='公開する';
+      }
     }
   }
   async function readArticle(id,{edit=false}={}){
@@ -42079,6 +42121,31 @@
   }
 }
 
+
+/* RC1.4.544: みてマガ長文記事はモーダル全体を単一スクロール領域にする。
+   body固定中でもホイール/タッチで最下部まで到達できるよう、カード内の二重スクロールを撤去。 */
+#miteMagazineReaderModal.mitemaga-modal{
+  overflow-y:auto!important;
+  overflow-x:hidden!important;
+  overscroll-behavior:contain!important;
+  -webkit-overflow-scrolling:touch!important;
+  touch-action:pan-y!important;
+  place-items:start center!important;
+}
+#miteMagazineReaderModal .mitemaga-reader-card{
+  max-height:none!important;
+  overflow:visible!important;
+  margin:auto!important;
+}
+#miteMagazineReaderModal #miteMagazineReader{
+  overflow:visible!important;
+  touch-action:pan-y!important;
+}
+@media(max-width:700px){
+  #miteMagazineReaderModal.mitemaga-modal{padding:7px!important;}
+  #miteMagazineReaderModal .mitemaga-reader-card{max-height:none!important;}
+}
+
 /* =========================================================
    RC1.4.342 みてマガ 投稿画面整理
    - 白/クリームの旧UIを撤去
